#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        int a, b, k;
        cin >> a >> b >> k;

        int diff = abs(a - b);
        int ans = (diff + k - 1) / k;   // ceiling division

        cout << ans << endl;
    }

    return 0;
}
